logic:
    Given the scenario description, what should I do given the instructions?

    Instructions: If people are fighting, say \"If you don't stop, I will call the police!\".

setup:
    sync_driver('perception')
    visual_desc = exec_driver('perception', 'read')
    output = logic('scenario description', visual_desc)
    
